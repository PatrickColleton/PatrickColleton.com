<nav>
  <span class="float_l"><%= link_to "Home", root_path %></span>
  <span class="float_l"><%= link_to "About", about_path %></span>
  <span class="float_l"><%= link_to "Download", download_path %></span>
  <% if !user_signed_in? %>
    <span class="float_l"><%= link_to "Sign Up", new_user_registration_path %></span>
    <span class="float_l"><%= link_to "Sign In", user_session_path %></span>
  <% end %>
  <% if user_signed_in? %>
      <span><%= link_to "Account Settings", edit_user_registration_path%></span>
      <span><%= link_to "News", posts_path%></span>
      <span><%= link_to "Sign Out", destroy_user_session_path, method: :delete %></span>
      <% if current_user.access_level == 'admin' %>
        <span class="float_r"><%= link_to "New Post", new_post_path %></span>
      <% end %>
  <% end %>
</nav>
